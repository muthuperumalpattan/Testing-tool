# API proxy must come first — Netlify uses first matching rule
[[redirects]]
  from = "/api/*"
  to = "https://testingbackend-xia0.onrender.com/api/:splat"
  status = 200
  force = true

# SPA fallback for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
